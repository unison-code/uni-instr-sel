entry:
  %const.-1.copy.1 = ##-1
  jump for.cond
bb0:
for.body:
for.inc:
  %mul = mpy(%res.0, %n.addr.0)
  %dec = add(%n.addr.0, %const.-1.copy.1)
for.cond:
  %n.addr.0 = PHI (%n, entry) (%dec, for.inc)
  %res.0 = PHI (%n, entry) (%mul, for.inc)
  %cmp = cmp.gt(%n.addr.0, 1)
  if (%cmp) jump bb0
bb1:
for.end:
  jumpr %res.0
